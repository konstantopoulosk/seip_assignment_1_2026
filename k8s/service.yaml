apiVersion: v1
kind: Service
metadata:
  name: echo-api-service
  labels:
    app: echo-api
spec:
  type: ClusterIP
  selector:
    app: echo-api
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
